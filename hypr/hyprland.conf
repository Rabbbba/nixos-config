# Hyprland config — Rayane
# https://wiki.hyprland.org/Configuring/
#
# Entry point — la config est splittée en modules sourcés ci-dessous.
# L'ordre de source compte : variables doivent être définies avant les binds qui les utilisent.

# ── Variables ─────────────────────────────────────────────────────────────────
$mod = SUPER
$terminal = footclient
$fileManager = footclient -e yazi
$browser = zen-beta
$menu = walker
$lock = hyprlock

# ── Environnement ─────────────────────────────────────────────────────────────
env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24
env = XCURSOR_THEME,Bibata-Modern-Amber
env = XKB_DEFAULT_LAYOUT,fr
# Workaround AMD/Mesa : EGL eglDupNativeFenceFDANDROID bug (fix flicker/tearing)
env = AQ_DRM_NO_EXPLICIT_SYNC,1

# ── Modules ───────────────────────────────────────────────────────────────────
source = ~/.config/hypr/modules/monitors.conf
source = ~/.config/hypr/modules/input.conf
source = ~/.config/hypr/modules/decoration.conf
source = ~/.config/hypr/modules/rules.conf
source = ~/.config/hypr/modules/binds.conf
source = ~/.config/hypr/modules/autostart.conf
