# Local notes — kept out of public history
CLAUDE.md
TODO.md

# Claude Code session state — local permissions, runtime metadata
.claude/

# pi-coding-agent — local AGENTS.md, prompts, mémoires (à la racine et dans les sous-dossiers)
.pi/
**/.pi/

# Nix build artifacts
result
result-*

# direnv local cache
.direnv/

# Editor swap files
*.swp
*.swo

# Generated by matugen on every wallpaper change — source of truth is matugen/templates/Theme.qml
quickshell/modules/Theme.qml

# Doxygen output (généré par `doxygen quickshell/Doxyfile`)
quickshell/doc/

# Auto-régénéré par Quickshell pour qmlls; contient des paths /nix/store/ avec
# des hashes spécifiques à un build, donc non portable.
quickshell/.qmlls.ini
